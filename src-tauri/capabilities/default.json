{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window may use the core APIs and ask for folders and confirmations. Nothing else: no shell, no arbitrary file access from the web view.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-ask"
  ]
}
